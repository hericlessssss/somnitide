package dev.somnitide.application.usecase;

/**
 * Stub — to be implemented in ETAPA 3.
 * Returns the preferences of the authenticated user (creates defaults if none
 * exist).
 */
public class GetPreferencesUseCase {
    // TODO (ETAPA 3): inject UserPreferencesRepository
    // TODO (ETAPA 3): implement execute(userId) -> UserPreferences
}
